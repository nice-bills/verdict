# Shared Agentathon-friendly demo market copy (sourced by e2e scripts).
DEMO_QUESTION="${DEMO_QUESTION:-Will Shannon testnet exceed 2 million transactions by June 2026?}"
DEMO_URL="${DEMO_URL:-https://somnia-testnet.blockscout.com}"
DEMO_RULE="${DEMO_RULE:-Use Blockscout network stats. Return YES if total transactions exceed 2,000,000, else NO. Return INVALID if stats are unavailable.}"
